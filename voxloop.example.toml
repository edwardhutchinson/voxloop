# VoxLoop's deployment file: everything needed to reach the store, and nothing an
# administrator can change from the console (ADR-0040). It is read once, at startup, and
# never re-read — so changing anything here, a certificate included, means a restart, and a
# restart ends every session.
#
# Every value can be overridden from the environment with a VOXLOOP_ prefix and a double
# underscore for the section: VOXLOOP_LISTEN__ADDRESS, VOXLOOP_STORE__PATH, and so on.
#
# Copy to voxloop.toml and edit.

[listen]
# One inbound TCP port carries HTTPS and the signalling WebSocket together.
# Optional; this is the default.
address = "0.0.0.0:8443"

[tls]
# TLS terminates in this binary — there is no reverse proxy anywhere in a VoxLoop
# deployment. HTTPS is mandatory even on a LAN, because a Secure cookie is not sent
# otherwise. A long-lived internal CA certificate suits a box whose renewal is a scheduled
# maintenance act. Required.
certificate = "/etc/voxloop/tls/certificate.pem"
private_key = "/etc/voxloop/tls/private-key.pem"

[media]
# Where the audio goes. **One** port carries every transport, UDP primarily with ICE-TCP
# fallback on the same number where UDP is blocked (ADR-0006) — a materially simpler firewall
# conversation than an ephemeral range, and there is no TURN server to configure because the
# box sits at a fixed address inside the network with remote users already on the VPN.
#
# `announced_address` is what goes into every ICE candidate, so it is the address a client
# dials rather than the one the box binds. It is the one value in this file with no default:
# get it wrong and VoxLoop comes up, serves the console and fills its seats while no audio
# ever arrives. Required.
announced_address = "10.0.0.4"
# What the worker binds. Optional; this is the default.
listen_address = "0.0.0.0"
# Optional; this is the default.
port = 44444

[connection]
# The signalling channel's health ladder, all four **in seconds** and all four optional;
# these are the defaults (v1 §7, ADR-0018). A session with no signalling channel has no
# emission path, and these say when that starts to be true.
#
# Every value has a hard ceiling and a number past it refuses to start rather than being
# quietly clamped. `disconnected` is a safety parameter rather than a display preference —
# it is how long a network hiccup may leave a microphone open that nobody's console can show,
# no loop can attribute and no authority holder can cut — so it must be tuned against this
# site's own VPN, and it cannot be turned into five minutes.
#
# The rungs have to climb: heartbeat, then unconfirmed, then disconnected.

# How often each end says it is still there.
heartbeat = 2
# How long a session goes unheard from before its console freezes its displayed state and
# marks it stale with a running age. Push-to-talk still stands here, deliberately: a single
# threshold would mute the Flight Director mid-sentence for a reroute that heals itself.
unconfirmed = 5
# How long a *latched* emission survives once the state is unconfirmed. A latch's whole
# safety story is that the console will show it to you, and that story is void the moment
# the console cannot be trusted.
latch_dropped = 2
# How long a session goes unheard from before emission is withdrawn at both ends: the client
# disables push-to-talk and the server closes the fan-out.
disconnected = 12

[store]
# The one SQLite file. The box is the database: there is no replica and no network
# endpoint, so backing this up is a real deployment obligation. Required.
path = "/var/lib/voxloop/voxloop.sqlite"

[log]
# A level, or a per-module list. Spans are targeted at the module that opened them, so
# "info,voxloop::media_plane=debug" turns up one part of the system without the rest.
# Optional; the default is "info".
level = "info"
